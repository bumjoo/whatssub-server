scalar DateTime

type Notification {
  id: ID!
  token: String!
  device: String
  os: String
  created: DateTime!
  updated: DateTime!
}

input NotificationCreateInput {
  userId: ID!
  token: String!
  device: String
  os: String
}
